# Tea More — E-card Demo

Static sales-menu demo for a tea shop.

## Local preview
Open `index.html` in a browser.

## Configure before real use
Edit `assets/js/tea-shop.js`:
- `shop.name`
- `shop.zaloUrl`

Images used by the page are in `assets/images/menu/`.
